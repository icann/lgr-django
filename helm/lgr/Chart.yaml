apiVersion: v1
appVersion: 1.0.0
description: A Helm chart for the LGR Tool Web Application to deploy to Kubernetes
name: lgr
type: application
# This is the chart version. This version number gets automatically incremented
# by the tagHelmChart method in the Jenkinsfile. There is no need to alter it manually.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
version: 0.1.0-1
